/** Bump this number whenever the rules content changes significantly enough
 *  to require all players to re-read them. */
export const RULES_VERSION = 1;
export const RULES_COOKIE  = `qwixx_rules_seen_v${RULES_VERSION}`;
