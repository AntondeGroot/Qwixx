# The freeze baseline (archunit_store/) is committed, so store creation is disabled: a missing store
# now fails loudly instead of silently re-baselining and accepting whatever cycles exist. To
# re-baseline deliberately (e.g. after breaking a cycle), flip this to true for one run, then back.
freeze.store.default.allowStoreCreation=false
